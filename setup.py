from setuptools import setup, find_packages

setup(
    name="SIPERlib",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    description="A simple motor control package using Raspberry Pi GPIO",
    classifiers=[
        "Programming Language :: Python :: 3",
        "Operating System :: OS Independent",
    ],
)
